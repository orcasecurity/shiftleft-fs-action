name: Test Orca FS action - Sarif

on:
  workflow_dispatch:

jobs:
  fs_scan_job:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v3
      - name: Scan FS
        id: orcasecurity
        uses: ./
        with:
          api_token:
            ${{ secrets.ORCA_SECURITY_API_TOKEN }}
          project_key:
            "default"
          path:
            "test"
          format:
            "sarif"
          output:
            "results/"
          console_output: "table"
      - name: print exit code
        if: always()  
        run: |
          echo "Exit code: ${{ steps.orcasecurity.outputs.exitcode }}"

      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@v2
        if: success() || failure()
        with:
          sarif_file: results/file_system.sarif